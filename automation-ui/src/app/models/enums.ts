export enum messageType {
    Success = 1,
    Warning = 2,
    Error = 3
}

export enum page {
    design = 1,
    enverionments = 2,
    monitor = 3,
    assess = 4,
}

export enum spinnerType {
    small = 0,
    fullScreen = 1
}
